# dependencies
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# next.js
.next/
out/
/build/

# misc
.DS_Store
*.pem
*.log

# env / secrets (NEVER commit in plaintext)
.env*
.gh-token
*.token

# NOTE: access/vault.zip IS committed (password-protected, per user decision).
# The password is NOT stored in this repo — only in the sandbox (.gh-vault-pass).
# See memory/05-github-workflow.md for the full decision.


# typescript
*.tsbuildinfo
next-env.d.ts

# editor
.vscode/
.idea/
